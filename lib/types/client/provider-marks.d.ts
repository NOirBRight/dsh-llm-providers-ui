/** Compact currentColor brand marks copied from each provider's BrandMark. DeepSeek fish geometry is copied once from the public SDK fish logo path and viewBox (published for consumers composing their own svg); the unknown-provider fallback is a generic currentColor globe. */
import type { ReactNode } from 'react';
export declare function ProviderMark(props: {
    providerKey: string;
}): ReactNode;
//# sourceMappingURL=provider-marks.d.ts.map